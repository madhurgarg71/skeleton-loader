import ImageCardLoader from "./ImageCardLoader";

export default function App() {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
      className="App"
    >
      <ImageCardLoader />
    </div>
  );
}
